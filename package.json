{
  "name": "travel-planner-dashboard-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run server",
    "dev:backend:db": "npm --prefix backend run db",
    "dev:backend:copilot": "npm --prefix backend run copilot",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "test": "npm --prefix backend run test && npm --prefix frontend run test",
    "test:contract": "npm --prefix backend run test:contracts",
    "test:e2e": "npm --prefix frontend run test:e2e",
    "test:visual": "npm --prefix frontend run test:visual",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "migrate:report": "npm --prefix backend run migrate:report",
    "clean:artifacts": "node scripts/clean.mjs artifacts",
    "clean:build": "node scripts/clean.mjs build",
    "clean:runtime": "node scripts/clean.mjs runtime --yes",
    "clean": "npm run clean:artifacts && npm run clean:build"
  }
}
