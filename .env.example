# This repo uses TWO .env files:
# - frontend/.env (Vite requires variables to start with VITE_)
# - backend/.env
#
# If you're setting up locally, copy the corresponding section(s) below
# into those files, or use backend/.env.example and frontend/.env.example.

############################
# Frontend (frontend/.env) #
############################

# Auth (Google)
VITE_GOOGLE_CLIENT_ID=

# Dev Server
VITE_PORT=3000

# API URLs
VITE_API_BASE_URL=http://localhost:3001
VITE_DB_API_URL=http://localhost:3002

###########################
# Backend (backend/.env)   #
###########################

# Auth (Google)
GOOGLE_CLIENT_ID=

# Optional: GitHub token for Copilot-related features
GITHUB_TOKEN=

# AI Provider Configuration
# gemini | ollama | local_api | copilot
AI_PROVIDER=copilot
OLLAMA_BASE_URL=https://example.ngrok-free.app

# Ports
PORT=3001
DB_SERVER_PORT=3002
COPILOT_SERVER_PORT=3003

# Internal service URLs
DB_SERVER_URL=http://localhost:3002
COPILOT_SERVER_URL=http://localhost:3003/process

# CORS
# You can use either FRONTEND_ORIGIN (single) or FRONTEND_ORIGINS (comma-separated).
FRONTEND_ORIGINS=http://localhost:5173,http://localhost:3000
